name: Continuous Integration

on:
  push:

env:

jobs:
  
  code:
    name: Code
    runs-on: [self-hosted]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

  build:
    name: Build
    runs-on: [self-hosted]
    steps:

  test: